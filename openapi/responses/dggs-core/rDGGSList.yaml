description: List of available Discrete Global Grid Reference Systems.
content:
  application/json:
    schema:
      type: object
      required:
        - dggs
      properties:
         links:
           type: array
           items:
             $ref: '../../schemas/common-core/link.yaml'
         dggs:
           type: array
           items:
              $ref: '../../schemas/dggs-core/dggs-item.yaml'
  text/html:
    schema:
      type: string
